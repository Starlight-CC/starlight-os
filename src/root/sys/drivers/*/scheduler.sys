--[[
Implements:

]]

local Scheduler = {}

function Scheduler.new()
    local self = {}
    function self.new()
        return Scheduler.new()
    end
    function self.queue(func,priority)
        return Scheduler.queue(self,func,priority)
    end
    function self.open(channel)
        return 
    self.stack = {}
    self.next = 1
    self.queue = {}
    self.channels = {}
    return self
end

function Scheduler